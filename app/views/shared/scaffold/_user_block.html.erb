<ul class="nav pull-right">
  <li class="dropdown">
    <a href="#" data-toggle="dropdown-toggle" class="dropdown-toggle">
      <%= t("labels.welcome")%>, <strong><%= current_user.name %></strong> <i class="icon-angle-down"></i>
    </a>
    <ul class="dropdown-menu">
      <li>
        <%= link_to(fa_icon('file-text-alt', text: '用户资料'), edit_user_path(current_user)) %>
      </li>
      <li>
        <%= link_to( fa_icon('signout', text: t("buttons.sign_out")), destroy_user_session_path, :method=>'delete') %>
      </li>
    </ul>
  </li>

  <% if !current_user.is_sys_admin? && current_user.spaces.count > 1 %>
  <li class="dropdown">
    <a href="#" data-toggle="dropdown-toggle" class="dropdown-toggle">
      <%= current_space.name %> <%= fa_icon('angle-down') %>
    </a>
    <ul class="dropdown-menu">
      <% current_user.spaces.each do |space| %>
      <li>
        <%= link_to(fa_icon('ok', text: space.name), nil) if space.id == current_space.id %>
        <%= link_to(" #{space.name}", switch_space_path(space)) unless space.id == current_space.id %>
      </li>
      <% end %>
    </ul>
  </li>
  <% end %>
</ul>
